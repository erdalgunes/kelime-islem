name: Simple Test

on:
  push:
    branches: [ fix/android-sdk-workflow-setup ]

jobs:
  test:
    name: Simple Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Test
        run: echo "Hello World"