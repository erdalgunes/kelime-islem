# This workflow is completely disabled
# The claude-code-action@v1 referenced in the original workflow does not exist
# Keeping this file as a placeholder to document the intended functionality

# Future implementations should use actual working Claude Code actions when available

name: Claude Code Review (Disabled)

# No triggers - this workflow will never run
on: []

# No jobs defined - this workflow is effectively disabled
jobs: {}